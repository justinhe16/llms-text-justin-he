"""Feature modules.

Each feature is `app/features/{feature}/` with `schemas.py`, `service.py`, and
`internals/{feature}_reader.py` + `internals/{feature}_writer.py` (ARCHITECTURE.md §3.1).
Empty until the first feature ticket lands.
"""
